--- a/index.html
+++ b/index.html
@@ -1257,6 +1257,9 @@ function openSyncSettings() {
       syncConfig.autoSync = autoSync;
       localStorage.setItem('github_token', token);
       localStorage.setItem('auto_sync', autoSync);
+      
+      console.log('💾 동기화 설정 저장:', { token: token.substring(0, 10) + '...', autoSync, gistId: syncConfig.gistId });

       // 초기 동기화
       if (autoSync) {
@@ -1315,6 +1318,11 @@ updateHome(); updateBadges(); setFilter('all');
 // 페이지 로드 시 자동 동기화 실행
 setTimeout(() => {
   if (typeof syncFromGist === 'function' && syncConfig && syncConfig.autoSync && syncConfig.token) {
+    console.log('🔄 페이지 로드: 자동 동기화 시작', {
+      hasFunction: typeof syncFromGist === 'function',
+      autoSync: syncConfig.autoSync,
+      hasToken: !!syncConfig.token
+    });
     syncFromGist().then(() => {
       console.log('✅ 페이지 로드: Gist에서 데이터 동기화 완료');
       updateHome(); updateBadges();
